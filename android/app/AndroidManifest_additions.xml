<!-- AndroidManifest.xml additions for presigned S3 + CameraX + Lottie + location + media -->

<!-- Add these permissions inside <manifest> but outside <application> -->
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

<!-- Add these inside <application> tag -->

<!-- FileProvider for sharing/accessing app-specific files -->
<provider
    android:name="androidx.core.content.FileProvider"
    android:authorities="${applicationId}.fileprovider"
    android:exported="false">
    <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/provider_paths" />
</provider>

<!-- Google Maps API key (if using maps in dashboard) -->
<meta-data
    android:name="com.google.android.geo.API_KEY"
    android:value="YOUR_GOOGLE_MAPS_API_KEY" />

<!-- Firebase Cloud Messaging (for push notifications) -->
<service
    android:name=".notifications.MyFirebaseMessagingService"
    android:exported="false">
    <intent-filter>
        <action android:name="com.google.firebase.MESSAGING_EVENT" />
    </intent-filter>
</service>

<!-- WorkManager initialization (auto-initialized, but can be configured here) -->
<!-- WorkManager will auto-initialize on app startup -->

<!-- CameraX will auto-request permissions via framework -->
