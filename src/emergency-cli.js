console.log('\nℹ️  Emergency AI CLI removed.');
console.log('This project now runs a local TensorFlow model for inference.');
console.log('If you need a CLI, create a new TF-aware CLI that loads your model and runs inference.\n');
process.exit(0);
