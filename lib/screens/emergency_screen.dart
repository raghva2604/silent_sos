import 'package:flutter/material.dart';

/// UI removed by request. This file now contains a minimal placeholder
/// so other imports that reference `EmergencyScreen` keep compiling.
class EmergencyScreen extends StatelessWidget {
  const EmergencyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Emergency screen removed')),
    );
  }
}
